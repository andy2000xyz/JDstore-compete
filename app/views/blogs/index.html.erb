<h1><%= "blog_list" %></h1>

<ul>
  <% @blogs.each do |blog| %>
    <li>
      <%= link_to blog.title, blog_path(blog) %>
      <%= blog.created_at.strftime("%Y/%m/%d %I:%M %p") %>
    </li>
  <% end %>
</ul>
